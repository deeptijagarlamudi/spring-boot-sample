CREATE TABLE employee(employeeid INT PRIMARY KEY,
   employeename VARCHAR(50),
   employeeaddress VARCHAR(50),
   salary INT, createdDateTime DATE, createdUser  VARCHAR(50), updatedDateTime DATE, updatedUser  VARCHAR(50));